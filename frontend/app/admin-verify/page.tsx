'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {postJSON} from '../../lib/api';
export default function AdminVerify(){const [password,setPassword]=useState(''),[error,setError]=useState(''),router=useRouter();async function submit(e:FormEvent){e.preventDefault();setError('');try{await postJSON('/api/auth/admin/verify',{password});router.replace('/')}catch{setError('管理员验证失败。')}}return <main className="auth-page"><section className="auth-card"><div className="brand-mark">ADMIN</div><h1>管理员二次验证</h1><p className="muted">管理、数据和后续交易控制需要短时管理员授权。</p><form onSubmit={submit}><label>Admin password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required autoFocus/></label>{error&&<p className="auth-error">{error}</p>}<button className="button">VERIFY</button></form></section></main>}
